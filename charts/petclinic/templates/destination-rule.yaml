{{- if .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: petclinic
spec:
  host: {{ .Release.Name }}
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
{{- end }}
